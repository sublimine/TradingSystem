[pytest]
addopts = -q
testpaths = tests
markers =
    integration: pruebas que requieren infraestructura externa (broker/db/etc)
    live: pruebas que golpean APIs/mercado real
    mt5: pruebas que importan/usan MetaTrader
